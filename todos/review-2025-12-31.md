# Code Review Summary - 2025-12-31

## Review Agents Run
- Security Sentinel ✅
- Performance Oracle ✅
- Architecture Strategist ✅
- Pattern Recognition ✅
- Code Simplicity ✅
- Agent-Native ✅
- Data Integrity Guardian ✅

---

## 🔴 CRITICAL ISSUES (Must Fix)

### Security
1. **PowerShell Command Injection** - `cmd/commands.go:434-513`
   - User-controlled directory paths interpolated into PowerShell scripts
   - Risk: Arbitrary code execution on Windows

2. **Token Exposure in Logs** - `internal/tui/logger/logger.go:203`
   - Full request/response bodies logged, may contain Bearer tokens
   - Log files at `~/.claude2kiro/logs/*.log` with 0644 permissions

### Performance
3. **Unbounded Memory Growth** - `internal/tui/logger/logger.go:297-300`
   - Ring buffer only removes one entry when at capacity
   - Risk: Memory spikes during bursts

4. **O(n²) String Operations** - `internal/tui/logger/logger.go:324-327`
   - Multiple space collapse uses repeated string replacement
   - Can iterate 10-20 times for large bodies

5. **Unbounded HTTP ReadAll** - `main.go:867`
   - `io.ReadAll()` without size limits
   - Risk: OOM from large/malicious responses

6. **Token Refresh Race Condition** - `main.go:312, 852-859`
   - `getToken()` reads file without mutex protection
   - Risk: Reading partially written token file

### Data Integrity
7. **Non-Atomic Config Save** - `internal/config/config.go:167-185`
   - `os.WriteFile` directly overwrites config
   - Risk: Config corruption on crash

8. **Non-Atomic Token Save** - `main.go:1809, 1882, 2055`
   - Token file writes not atomic
   - Risk: User locked out if crash during write

9. **Unbuffered Log Writes** - `internal/tui/logger/logger.go:304`
   - No error handling, no buffering, no flush
   - Risk: Silent data loss on disk full

### Architecture
10. **main.go is 2,653 lines** - Contains 49 functions, 25 types
    - HTTP server, auth flows, token management, CLI all in one file
    - Should extract to internal/server/, internal/auth/, internal/token/

11. **Type Duplication** - TokenData defined in main.go AND cmd/commands.go
    - Also duplicate: CreditsInfo, TokenInfo, ClaudeConfigStatus, messages

---

## 🟡 IMPORTANT ISSUES (Should Fix)

### Security
12. **Insecure File Permissions** - `config/config.go:184`
    - Config/temp files use 0644 (world-readable)
    - Should use 0600 for sensitive files

13. **Missing HTTP Security Headers** - `main.go:792-795`
    - CORS allows all origins (`*`)
    - No CSP, X-Frame-Options, X-Content-Type-Options

14. **No Rate Limiting** - Proxy server `/v1/messages`
    - Risk: DoS, credit abuse, brute force

### Performance
15. **O(n) Filter Scan** - Every filter change rescans all entries
    - Should use incremental filtering with indices

16. **Unbuffered File I/O** - Each log entry = 1 syscall
    - Should use buffered writer with periodic flush

17. **No HTTP Connection Pooling** - New clients created inconsistently
    - Should reuse single http.Client with connection pool

### Architecture
18. **Message Type Duplication** - `internal/tui/messages.go` vs `messages/messages.go`
    - Two files define same types (ServerStartedMsg, ServerStoppedMsg)

19. **Circular Dependency Risk** - cmd ↔ dashboard potential cycle
    - Workaround via type aliases, fragile architecture

20. **Global Config State** - `config.Get()` uses unprotected global var
    - Race condition, testing difficulty

### Data Integrity
21. **No Concurrent Write Protection** - config.Save() has no mutex
22. **Silent Parse Errors** - config.Load() swallows YAML errors
23. **Session Metadata Race** - sessionMetadataMap mutated without lock

### Agent-Native (CLI Gaps)
24. **No `status --json`** - Cannot check if server is running programmatically
25. **No `credits --json`** - Credit info only in TUI
26. **No `logs --json`** - Log access only in TUI
27. **No health endpoint** - No `/health` for orchestration
28. **No graceful shutdown** - Must kill process, no SIGTERM handler

### Simplicity
29. **logviewer.go is 2,736 lines** - Too many responsibilities
30. **settings.go is 1,600 lines** - Should be split
31. **Weird `jsonStr` alias** - `import jsonStr "encoding/json"` (33 uses)
32. **Duplicate status panel rendering** - ~200 lines in menu.go AND dashboard.go

---

## 🔵 NICE-TO-HAVE (When Time Permits)

33. Magic numbers for column widths (use constants)
34. SSE parser creates many small allocations (use struct pool)
35. Log directory size walks entire tree (cache result)
36. Only 1 test file exists (add test coverage)
37. Modernize to use Go generics (58 uses of interface{})
38. Add Prometheus metrics endpoint
39. Add session metadata export for project-level tracking

---

## Recommended Action Phases

### Phase 1: Security & Stability (Immediate)
- Fix PowerShell injection (use parameter binding)
- Add token redaction to logger
- Change sensitive file permissions to 0600
- Implement atomic write-rename for config/token saves
- Add mutex to getToken() and config.Get()

### Phase 2: Performance (1-2 weeks)
- Add io.LimitReader to HTTP reads
- Fix O(n²) string sanitization
- Add buffered file I/O for logs
- Implement HTTP client connection pooling

### Phase 3: Architecture Refactoring (v0.3.0)
- Split main.go → internal/server/, internal/auth/, internal/token/
- Consolidate duplicate types to internal/types/
- Fix message type duplication
- Split logviewer.go into focused files

### Phase 4: Agent-Native CLI (v0.3.0)
- Add `status --json` command
- Add `credits --json` command
- Add `logs --tail N --json` command
- Add `/health` endpoint
- Implement graceful shutdown (SIGTERM handler)

---

## File Impact Summary

| File | Critical | Important | Nice-to-Have |
|------|----------|-----------|--------------|
| main.go | 4 | 2 | 1 |
| cmd/commands.go | 1 | 1 | 0 |
| internal/tui/logger/logger.go | 3 | 2 | 0 |
| internal/config/config.go | 1 | 3 | 1 |
| internal/tui/dashboard/logviewer.go | 0 | 2 | 1 |
| internal/tui/dashboard/dashboard.go | 0 | 1 | 0 |

---

## Code Metrics

| Metric | Current | Target |
|--------|---------|--------|
| main.go LOC | 2,653 | <300 |
| logviewer.go LOC | 2,736 | <500 |
| Duplicate struct definitions | 6+ | 1 each |
| Test coverage | ~5% | >60% |
| Agent-native capabilities | 50% | 95% |
| Critical security issues | 2 | 0 |

---

*Generated by multi-agent code review - 2025-12-31*
